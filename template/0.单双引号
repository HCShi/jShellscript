#!bin/bash
##################################################################
## 单双引号
## 这里有将pwd的结果转化为字符串的
## 用双引号可以直接转，用单引号就不行
##################################################################
cd
path=`pwd`
echo "$path/.ssh"  # /home/coder352/.ssh
echo '$path/.ssh'  # $path/.ssh
