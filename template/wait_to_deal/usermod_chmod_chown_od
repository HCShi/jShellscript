#!/bin/bash

##################################################################
## usermod chmod chown
##################################################################
sudo usermod -a -G root coder352

sudo chown -R hadoop ./hadoop  # 将当前的 hadoop 文件夹的权限改为 hadoop 用户, -R 表示递归
chown root:root aa             # 修改aa的用户和组

chmod a-w aa  # 修改aa的那9个属性
sudo chmod 777 filename
sudo chmod 600 filename

##################################################################
## 代码 编码
##################################################################
# 将 代码 全部放到 log.txt 中,可以看到 全角的中文字符
# gcc编译报错：程序中有游离的‘\357’‘\273’‘\277’等, 这个错误可以解决
# 程序（＊.c，＊.h）中使用了中文的标点符号(全角)，比如；，｝，＋。
# 改成英文的标点半角符号就行了。
# 甚至有时候空格也会出现类似错误，删掉该空格 重新输入
od -c shell.c > log.txt

# 查看 代码 , 以 十六进制形式
cat shell.c | hd
# 这样会显示跟十六进制查看器一样的效果
